
import Foundation

// Creating an array
var numbers: [Int] = [1, 2, 3, 4, 5]

// Adding elements
numbers.append(6)

// Accessing elements
let firstNumber = numbers[0]

// Iterating over an array
for number in numbers {
    print(number)
}
